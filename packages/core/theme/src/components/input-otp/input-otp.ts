import type {VariantProps} from "tailwind-variants";

import {tv} from "../../utils/tv";

/**
 * InputOTP wrapper **Tailwind Variants** component
 */
const inputOTP = tv({
  base: [],
  variants: {},
  defaultVariants: {},
});

export type InputOTPVariantProps = VariantProps<typeof inputOTP>;

export {inputOTP};
