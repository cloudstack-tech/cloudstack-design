---
description: Cloudstack Design — a modern React component library built with TypeScript, TailwindCSS, and Tsup.
globs: *
alwaysApply: true
---

1. This is a component library which is used in cloud compute.
2. Don't use tailwind default color as theme, here is themes package in `src/packages/core/theme`.
